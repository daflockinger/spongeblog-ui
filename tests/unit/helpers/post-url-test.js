import { postUrl } from 'spongeblog-ui/helpers/post-url';
import { module, test } from 'qunit';

module('Unit | Helper | post url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postUrl([42]);
  assert.ok(result);
});
